import { featureKey, State, reducer } from '@store/settings/settings.reducer';
import * as fromSettings from '@store/settings/settings.selectors';

export { featureKey, State, reducer, fromSettings };

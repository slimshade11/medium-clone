import * as FeedActions from '@store/feed/feed.actions';
import { featureKey, reducer, State } from '@store/feed/feed.reducer';
import * as fromFeed from '@store/feed/feed.selectors';

export { featureKey, reducer, State, fromFeed, FeedActions };
